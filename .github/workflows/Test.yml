---
name: Ole

on:
  pull_request:
    branches: [ main ]
#on:
  #workflow_run:
    #workflows:
      #- Benny
    #types:
      #- completed

jobs:
  test-job:
    name: Test Step
    runs-on: ubuntu-latest
    steps:
    - name: Echo Something
      run: echo "Hello, this is an example echo!"
